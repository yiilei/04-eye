# 采光发布前故障矩阵

`自动` 表示可以由测试或程序确定；`人工` 表示发布前必须用真实 macOS、小红书或 Eagle 操作一次。

| 环节 | 情况 | 应有结果 | 验证 |
|---|---|---|---|
| 安装 | 全新安装 / 覆盖旧版 | 应用可启动；覆盖不动用户资料 | 人工 |
| 安装 | 缺脚本、错架构、损坏压缩包 | 安装前或启动前明确失败，不删旧版 | 自动+人工 |
| 更新 | 下载断网、链接伪造、版本不匹配、签名失败 | 拒绝安装；保留当前版；替换失败自动回滚 | 自动+人工 |
| 首次使用 | 页面矮、白天/黑夜、文字变长 | 按钮不遮挡，说明可滚动，未完成前不误进批阅 | 人工 |
| 登录 | Chrome 已登录 / 多 profile / 未登录 / Cookie 被锁 | 只在用户点击后只读复制；失败转独立扫码；不循环弹窗 | 人工 |
| 登录 | 过期、二维码过期、验证码、风控 | 限时退出并提示重新登录，不标记任务完成 | 自动+人工 |
| Eagle | 未安装 / 未启动 / 401 / 超时 / 文件夹不存在 | 可继续抓取；YES/X 明确失败并保留本地文件；不假成功 | 自动+人工 |
| Eagle | YES 或 X 连点 | 只导入一次；完成前禁用重复操作 | 自动+人工 |
| 埋点 | 同名不同人、改名、主页失效、待验证 | 只按 profileId/profileUrl 核验；不按相似名换绑 | 自动+人工 |
| 发现 | 无新增 / 多篇新增 / 置顶 / 重名活动 | 以稳定 ID 去重，不以标题去重，不回填旧历史 | 自动 |
| 发现 | 慢查询时用户同时 YES/NO | 写入前重读最新队列，不用旧快照覆盖用户决定 | 自动 |
| 活动 | 抽屉 iframe、路径改名、正文未发布、返回 JSON 错误 | 按 activityId 读真实 iframe；保留封面、原入口和失败原因；稍后补抓 | 自动+人工 |
| H5 | 超长、懒加载、GIF/WebP/MP4、动效、空白段、推荐流 | 等待稳定后分段校验；保留顺序和动态素材；裁掉非正文推荐 | 自动+人工 |
| 媒体 | 过期 URL、0 字节、尺寸异常、Live Photo 缺配对、视频为空 | 校验不通过不进批阅；错误项保留任务与证据等待重试 | 自动 |
| 调度 | 关机、休眠、过时开机、数日未开机 | 不承诺关机/合盖运行；下次开机或启动采光时补一次 | 自动+人工 |
| 调度 | 定时、手动、唤醒同时触发 / 进程崩溃留锁 | 活进程互斥；残留锁在确认进程消失后恢复 | 自动 |
| 网络 | 断网、DNS、HTTP 429/401/5xx、请求卡住 | 有限超时、分类说明、冷却后最多补试；不快速无限重试 | 自动+人工 |
| 批阅 | YES、NO、撤回、重启、无素材 | YES 导入后才记录；NO 先进可恢复区；窗口骨架保持不跳页 | 自动+人工 |
| 存储 | 磁盘满、无权限、JSON 损坏、路径越界 | 原子写入；删除限制在采光资料库；失败时不误删 | 自动+人工 |
| 界面 | 长文、长错误、小窗口、两种主题 | 不重叠按钮/底栏；错误可换行且可关闭 | 人工 |
| 隐私 | 源码、日志、问题报告 | 不提交 Cookie、用户资料、本地素材或 Eagle 内容 | 自动+人工 |

## 每次 Release 必须通过

1. Node 全量测试、Python H5/会话测试、生产构建、`git diff --check`。
2. 打包后检查所有本地 import，再执行 `codesign --verify --deep --strict`。
3. 用全新资料目录走完：登录、Eagle 检测、首次抓取、YES、NO+撤回、X 截图、无素材页。
4. 用保留资料覆盖升级，确认埋点、队列、登录、素材和批阅决定都在。
5. 断网各走一次登录、抓取、YES、更新；关闭 Eagle 后走 YES 和 X。
6. Release 的应用版本、tag、安装包文件名三者一致，源码归档可下载。

## 不可能被“穷举”消灭的外部情况

小红书页面和风控、网络、macOS 权限、Eagle API 未来变更无法预先穷举。这些分支的验收标准不是“永远成功”，而是：有限等待、不误报成功、不破坏旧数据、保留可诊断证据、告诉用户下一步。
