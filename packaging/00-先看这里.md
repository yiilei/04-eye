# 采光安装说明

1. 完整解压本安装包，不要直接在压缩包预览中运行。
2. 双击“开始安装.command”。它会把应用放入 `~/Applications`，把源码放入采光本地资料目录，并安装采集引擎与本地定时器。
3. 首次打开采光，按引导登录小红书；Eagle 可以连接，也可以暂时跳过。
4. 如果 macOS 阻止打开，请在访达中右键“采光.app”并选择“打开”。

安装器不会携带作者 Cookie、私人素材或 Eagle 数据，也不会修改 Chrome 中的登录状态。
