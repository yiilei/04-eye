# v0.3.45 验收记录

当前定位：本地候选版本，尚不能宣称六项验收全部完成。

## 已验证

- 删除事务的三个中断阶段可恢复；后续 HTTP 删除先恢复旧日志。
- NO 后立即撤回恢复文件；清理后撤回返回 410 和不可恢复原因。
- 死 PID 锁立即回收、活 PID 等待、空锁有短暂宽限。
- 单张状态的文件标识在图片重排后保持对应；迁移空状态不能覆盖已初始化资料。
- Eagle 标签分页查找排除回收站项目；核对服务失败或分页未完成抛出待确认错误。
- 超过六批的模拟发现可保存进度并继续；基线持续缺失进入明确待处理状态。
- 前一轮完整测试：22 项 Python 测试、104 项 Node 测试通过；后续修订的定向测试通过。
- TypeScript 检查与生产构建通过。
- 隔离资料测试：全新资料种入 1 条初始素材；保留资料启动保存旧媒体、偏好和单张记录。

## 尚需完整验收，不能据此宣称零故障

- 真实桌面界面完成单张删除、另一张保存、退出重开、整篇 YES 的连续流程；目前文件标识测试与 HTTP 持久化测试分开验证。
- JavaScript 持锁子进程被 SIGKILL 后的立即恢复已通过；Python 与 JavaScript 同时争用同一份真实用户资料仍未做破坏性联调。
- 真实 Eagle 导入的部分成功、实际超时后成功、关闭应用和回收站组合测试；目前使用可控返回值验证策略。
- 最终 ZIP 已通过解压、签名结构校验，以及包内服务的全新资料和保留资料启动测试；仍未把完整安装器向导和所有 Electron 点击流程自动化。

## 外部边界

平台没有可靠公开续页游标时采用有界扩大扫描窗口。原基线找不到时不会推进成功基线，也不保证已经补齐；需要继续核查基线是否删除或隐藏。

签名为本地 ad-hoc 签名，严格文件校验通过不等于 Apple Developer ID 公证。
