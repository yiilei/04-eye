# 04的眼 v0.1.1

这是第一个真正打通“抓取 → 本地资料库 → 自动出现批阅内容 → Eagle / 回收区”的版本。

## 本次更新

- 抓取脚本与桌面应用改为共用 `~/Library/Application Support/04的眼/`。
- 抓取完成后，已经打开的应用会在约 2 秒内自动进入批阅页，无需刷新或重启。
- 图片、完整长图、普通视频与 Live Photo 均从本地资料库加载，不依赖易失效的远程素材地址。
- NO 会把整组素材移入应用回收区；`Command + Z` 同时恢复文件和批阅条目。
- YES 会执行清晰度校验，再导入 Eagle；“小红书”文件夹不存在时自动创建。
- 抓取时间、推送时间和用户选中的账号埋点写入本机配置，供 Codex 自动任务读取。
- 修复空资料库、CSS 加载、运行时新增内容和长视频分段读取问题。

## 使用方式

1. 下载并解压 `04的眼-macOS-arm64.zip`。
2. 把应用拖入“应用程序”；首次被 macOS 拦截时，在访达中右键应用并选择“打开”。
3. 启动 Eagle（可以暂时跳过）。
4. 把仓库链接发给 Codex，并说：`请安装并配置04的眼，然后帮我抓取最新的小红书创作服务活动`。

当前公开测试包仅支持 Apple Silicon Mac，并使用临时签名，尚未进行 Apple 公证。
