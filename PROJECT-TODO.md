# 04的眼 · 待解决问题

## 执行原则

- 新功能和技术故障先检索现有开源工具、官方资料与成熟实现，再决定复用、改造或自研；记录选择理由后再修改程序。
- 账号身份以稳定主页内部 ID 与小红书号为准；昵称变化只更新名称并保留旧名称记录，不重新绑定账号。

## 画布显示

- [x] 宽幅素材的默认画布比例
  - 触发条件：素材为真正的横向图片，即 `宽 ÷ 高 > 1`。
  - 预期效果：进入素材或切换组图时，宽幅画板的默认宽高固定为 `5:3`。
  - 宽幅图片完整约束在 `5:3` 画板内，不再沿水平方向溢出或出现横向滚动。
  - 方图与竖图：恢复按素材自身比例适配；超长竖图最低以 `9:16` 画板滚动展示。
  - 保持不变：用户在画布内部进行的缩放、平移和同组图片缩放记忆不受影响。
  - 验收边界：分别检查恰好 `3:5`、略宽于 `3:5`、`4:3`、`16:9` 和超宽幅素材，确保切换后不出现左右异常空隙或画布比例跳动。
  - 完成日期：2026-08-14。

- [x] 区分窗口边缘与角落的拉伸方向
  - 上、下、左、右边缘：仅改变画布对应方向的展示面积，左右栏宽度保持不变。
  - 四个角落：同时改变整个窗口的宽度和高度。
  - 完成日期：2026-08-14。

- [x] 画布专注模式
  - 快捷键：`Command + .`。
  - 预期效果：同时隐藏左右两栏和栏内控件，只保留中间画布；再次按下恢复进入前的栏状态。
  - 完成日期：2026-08-14。

- [x] 删除当前整组素材快捷键
  - 快捷键：`Command + Backspace`。
  - 预期效果：从批阅页面和左侧轮播中直接移除当前整组素材，并自动切换到下一组；刷新页面后保持移除状态。
  - 撤回：`Command + Z` 恢复刚刚移除的整组素材及原位置。
  - 完成日期：2026-08-14。

## 设置

- [x] 小红书账号埋点管理
  - 设置页保留抓取时间与推送时间，在下方增加可滚动的账号列表。
  - 支持按账号名称、小红书号和检索标识搜索。
  - 已载入现有 10 个固定埋点账号；支持“埋点 / 取消埋点”，选择会在本机保留。
  - 搜索无本地结果时可直接跳转小红书“用户”搜索；支持一键导出当前埋点 JSON。
  - 顶部使用红色小红书用户搜索框；下方可粘贴账号主页链接并直接埋点，帖子链接会被拦截。
  - 新主页先以固定主页 ID 建立待核验埋点，首次自动检查时再补齐昵称、小红书号与头像。
  - 10 个账号头像已从各自固定主页核验并保存至本地，导出数据同时保留头像来源和本地路径。
  - 完成日期：2026-08-14。
